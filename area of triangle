#include <stdio.h>
// area of circle
float areaofTriangle(){
   float x, y;
      printf("Enter base :");
      scanf("%f", &x);
      printf("Enter height: ");
      scanf("%f",&y);
      return 0.5*x*y;   
}
void main(){
float ans;
ans = areaofTriangle();
printf("%.2f",ans);
   }
